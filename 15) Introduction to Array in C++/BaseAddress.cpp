// Base Address: Base Address of an array is it's first address. We can find the base address of an array as follows.

#include<iostream>
using namespace std;

int main(){
    
    int arr[5];
    
    cout<<arr<<endl;
    cout<<&arr<<endl;

    return 0;
}